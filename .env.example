# Supabase (required for Pacelab)
# Anon key MUST be from Dashboard → Project Settings → API → "anon" "public" (long JWT starting with eyJ).
# Wrong key → "invalidJWT" when calling Edge Functions.
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Strava OAuth (app: Connect with Strava; callback is Supabase Edge Function)
EXPO_PUBLIC_STRAVA_CLIENT_ID=your-strava-client-id

# Groq (AI coach chat — Llama 3.3 70B)
EXPO_PUBLIC_GROQ_API_KEY=your-groq-api-key

# AI Coaching Engine (Supabase Edge Function secrets)
# Set these in Supabase Dashboard → Project Settings → Edge Functions → Secrets
# GROQ_API_KEY=your-groq-api-key
# OPENWEATHER_API_KEY=your-openweather-api-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Edge Functions used by coaching engine:
# - calculate-fitness-metrics (CTL/ATL/TSB, VO2max, thresholds)
# - ingest-athlete-data (unified AthleteState)
# - detect-bottleneck (8-signal bottleneck detection)
# - select-philosophy (bottleneck → training mode)
# - generate-training-plan (Groq-powered plan with diagnosis context)
# - run-adaptation-loop (weekly Monday analysis, cron)
# - optimize-daily-session (morning session optimizer, cron)
# - analyze-run (post-run AI summary + PR detection)
